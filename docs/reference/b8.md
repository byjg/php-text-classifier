---
sidebar_position: 1
---

# B8 Class

`B8\B8` is the binary Robinson-Fisher Bayesian spam filter.

## Constructor

```php
new B8(ConfigB8 $config, StorageInterface $storage, LexerInterface $lexer)
```

| Parameter | Type | Description |
|---|---|---|
| `$config` | `ConfigB8` | Tuning parameters |
| `$storage` | `B8\Storage\StorageInterface` | Persistence backend |
| `$lexer` | `B8\Lexer\LexerInterface` | Tokeniser |

## Methods

### classify()

```php
public function classify(string|null $text): float|string
```

Classifies `$text` and returns a spam probability.

| Return type | Meaning |
|---|---|
| `float` between `0.0`–`1.0` | Probability score. `0.0` = ham, `1.0` = spam |
| `B8::CLASSIFYER_TEXT_MISSING` | `$text` was `null` |
| `StandardLexer::LEXER_TEXT_NOT_STRING` | `$text` is not a string |
| `StandardLexer::LEXER_TEXT_EMPTY` | `$text` is an empty string |

Always check that the return value is a `float` before using it numerically:

```php
$score = $b8->classify($text);
if (is_float($score) && $score > 0.8) {
    // spam
}
```

### learn()

```php
public function learn(string|null $text, string|null $category): null|string
```

Trains the classifier with `$text` as a known example of `$category`.

| Parameter | Accepted values |
|---|---|
| `$category` | `B8::SPAM` or `B8::HAM` |

Returns `null` on success or an error code string on failure. See [Error Codes](error-codes.md).

### unlearn()

```php
public function unlearn(string|null $text, string|null $category): null|string
```

Reverses a previous `learn()` call. Parameters and return values identical to `learn()`.

## Constants

### Category constants

| Constant | Value |
|---|---|
| `B8::SPAM` | `'spam'` |
| `B8::HAM` | `'ham'` |

### Action constants (internal)

| Constant | Value |
|---|---|
| `B8::LEARN` | `'learn'` |
| `B8::UNLEARN` | `'unlearn'` |

### Error code constants

| Constant | Returned by |
|---|---|
| `B8::CLASSIFYER_TEXT_MISSING` | `classify()` |
| `B8::TRAINER_TEXT_MISSING` | `learn()`, `unlearn()` |
| `B8::TRAINER_CATEGORY_MISSING` | `learn()`, `unlearn()` |
| `B8::TRAINER_CATEGORY_FAIL` | `learn()`, `unlearn()` |

### Internal

| Constant | Value | Purpose |
|---|---|---|
| `B8::DBVERSION` | `3` | Expected database schema version |

## Related

- [ConfigB8 reference](config-b8.md)
- [How B8 works](../concepts/how-b8-works.md)
- [Storage backends](../concepts/storage-backends.md)
- [Error codes](error-codes.md)
